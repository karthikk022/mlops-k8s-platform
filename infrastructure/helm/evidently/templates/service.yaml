apiVersion: v1
kind: Service
metadata:
  name: evidently
  namespace: {{ .Release.Namespace }}
  labels:
    app: evidently
spec:
  type: ClusterIP
  ports:
    - port: 8085
      targetPort: 8085
      name: http
  selector:
    app: evidently
